#include <unistd.h>
#include <stdio.h>

int main(int av, char **ac)
{
    int fd[2];

    if (av == 5)
    {
		// learn fork()
		// learn pipe()
		// file manupilation : read() & write() & open() & close()
		// command excution : execvp() & execve()
		// error handling
		// 

    }
}